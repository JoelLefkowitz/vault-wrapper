[tox]
envlist = 
    py36
    py37
    py38

[testenv]
deps = 
    .[tests]
    pytest
    pytest-cov
    
commands = 
    coverage run --include vault-wrapper/* -m    \
    pytest --doctest-modules vault-wrapper    

    coverage run --include vault-wrapper/* -a -m \
    pytest

    coverage report
    coverage xml
